import { ChangesBackground } from './changes-background';

describe('ChangesBackground', () => {
  it('should create an instance', () => {
    const directive = new ChangesBackground();
    expect(directive).toBeTruthy();
  });
});
