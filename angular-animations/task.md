- When to use angular animations and not css animations?
- How to animate the following example:

<button (click)="fadeInOut()">Fade in / out <button>
<div *ngIf="isShown">This is a block with fade In / Out</div>
